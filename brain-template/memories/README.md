One fact per file. Example: `prefers-plain-language-answers.md`.
Frontmatter (`name`, `description`, `type`, `source`, `captured`) + free markdown body.
Types: `user`, `feedback`, `project`, `reference`.
Add `visibility: local` to any item that must never leave this machine.
