One topic or reference page per file. Example: `react-hooks-mental-model.md`.
Files have frontmatter (`name`, `description`, `type: knowledge`, `visibility`) + free
markdown body. `visibility:` is required — `shared` or `local`, same rule as `memories/`.
